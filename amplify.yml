version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd pdf_ui
        - npm ci
    build:
      commands:
        - cd pdf_ui
        - npm run build
  artifacts:
    baseDirectory: pdf_ui/build
    files:
      - '**/*'
  cache:
    paths:
      - pdf_ui/node_modules/**/*
